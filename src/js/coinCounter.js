const coinCounter = (amount) => {
  const coinValues = {
    quarter: 0.25,
    dime: 0.10,
    nickel: 0.05,
    penny: 0.01
  };
  console.log(coinValues);

  const coinNumbers = {
    quarters: 0
  };

  console.log(coinNumbers);
  return amount;
}

export default coinCounter;

