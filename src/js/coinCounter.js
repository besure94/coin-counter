const coinCounter = (amount) => {
  const coinValues = {
    quarter: 0.25,
    dime: 0.10,
    nickel: 0.05,
    penny: 0.01
  };
  console.log(coinValues);
  return amount;
}

export default coinCounter;

